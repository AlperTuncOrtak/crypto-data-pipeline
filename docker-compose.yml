version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: crypto_mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: "${MYSQL_ROOT_PASSWORD:-12345678}"
      MYSQL_DATABASE: "${MYSQL_DATABASE:-crypto_analysis}"
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./sql/schema.sql:/docker-entrypoint-initdb.d/schema.sql

  redis:
    image: redis:7-alpine
    container_name: crypto_redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  api:
    build: .
    container_name: crypto_api
    restart: always
    command: uvicorn backend.main:app --host 0.0.0.0 --port 8000
    env_file:
      - ./backend/.env
    environment:
      - MYSQL_HOST=mysql
      - REDIS_HOST=redis
    ports:
      - "8000:8000"
    depends_on:
      - mysql
      - redis

  ws_worker:
    build: .
    container_name: crypto_ws_worker
    restart: always
    command: python src/multi_exchange_ws.py
    env_file:
      - ./backend/.env
    environment:
      - MYSQL_HOST=mysql
      - REDIS_HOST=redis
    depends_on:
      - mysql
      - redis

volumes:
  mysql_data:
  redis_data:
